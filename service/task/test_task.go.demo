package task

import (
	"app/conf"
	"github.com/sohaha/zlsgo/ztime"
	"github.com/sohaha/zlsgo/ztime/cron"
)

// RegDemo 注册定时任务示例
func (*cornTask) RegDemo(c *cron.JobTable) {
	_, _ = c.Add("* * * * *", func() {
		conf.Log.Debug("每分钟执行一次:", ztime.Now())
	})
}
